[project]
name = "lab2-new"
version = "0.1.0"
description = "Regex -> NFA -> DFA -> MinDFA (Thompson + subset + minimization) with Streamlit UI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40.0",
]

